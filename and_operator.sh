#!/bin/bash

# AND operator = &&
# syntax = command1 && command2
# command2 is executed if, and only, command1 runs sucessfully which means return an exit status of zero
# example
# mkdir and_dir && cd and_dir // create folder and_dir, if folder creation succesful, then only change the directory
# cd dir_dir && ls -al

 
